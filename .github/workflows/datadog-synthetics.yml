name: Datadog Synthetic tests

on:
  workflow_dispatch:

jobs:
  synthetics:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Run Datadog Synthetic tests
      if: ${{ secrets.DD_API_KEY != '' && secrets.DD_APP_KEY != '' }}
      uses: DataDog/synthetics-ci-github-action@87b505388a22005bb8013481e3f73a367b9a53eb
      with:
        api_key: ${{ secrets.DD_API_KEY }}
        app_key: ${{ secrets.DD_APP_KEY }}
        test_search_query: 'tag:e2e-tests'
    - name: Skip notice
      if: ${{ secrets.DD_API_KEY == '' || secrets.DD_APP_KEY == '' }}
      run: echo "::warning::Datadog secrets (DD_API_KEY, DD_APP_KEY) not configured. Tests skipped."
